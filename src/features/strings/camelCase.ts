export const camelCase = (str: string): string => {
    const camelCasedStr: string = "";

    // Loop through the string

    // Find the index for the occurence of " "

    // Remove space and capatilize the words following the space
    return camelCasedStr;
}